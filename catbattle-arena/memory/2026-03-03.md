# Daily Notes

- Fixed Gallery → Cat detail back behavior by adding `from=gallery` when opening a cat and using `router.back()` with a `/gallery` fallback on the cat profile page.
- Fixed inflated win/loss/battle counts on cat profile by making match-count queries explicit `or(and(...),and(...))` branches (avoids filter-precedence surprises with `.or(...)`).
- `npm run lint` currently fails because Next 16 CLI no longer exposes `next lint` in this project setup; used `npx tsc --noEmit` instead.

