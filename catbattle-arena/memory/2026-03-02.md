# Daily Notes
- 2026-03-02: New session started to troubleshoot arena reload bug and add a /debug reset button.
- Fixed TS build break from `/dev/match-card` importing `MatchCard` from `app/page.tsx` by swapping to a local preview component.
- DebugWidget refresh now clears local vote state without wiping the deck, forces queue info to "fresh" (not complete), and remounts ArenaSection to exit results view.
