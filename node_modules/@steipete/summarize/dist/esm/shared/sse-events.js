export function encodeSseEvent(event) {
    return `event: ${event.event}\ndata: ${JSON.stringify(event.data)}\n\n`;
}
export function parseSseEvent(message) {
    switch (message.event) {
        case 'meta':
            return { event: 'meta', data: JSON.parse(message.data) };
        case 'slides':
            return { event: 'slides', data: JSON.parse(message.data) };
        case 'status':
            return { event: 'status', data: JSON.parse(message.data) };
        case 'chunk':
            return { event: 'chunk', data: JSON.parse(message.data) };
        case 'assistant':
            return { event: 'assistant', data: JSON.parse(message.data) };
        case 'metrics':
            return { event: 'metrics', data: JSON.parse(message.data) };
        case 'done':
            return { event: 'done', data: JSON.parse(message.data) };
        case 'error':
            return { event: 'error', data: JSON.parse(message.data) };
        default:
            return null;
    }
}
//# sourceMappingURL=sse-events.js.map