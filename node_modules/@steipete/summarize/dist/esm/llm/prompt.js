export function userTextMessage(text, timestamp = Date.now()) {
    return { role: 'user', content: text, timestamp };
}
function bytesToBase64(bytes) {
    return Buffer.from(bytes).toString('base64');
}
export function userTextAndImageMessage({ text, imageBytes, mimeType, timestamp = Date.now(), }) {
    const parts = [
        { type: 'text', text },
        { type: 'image', data: bytesToBase64(imageBytes), mimeType },
    ];
    return { role: 'user', content: parts, timestamp };
}
//# sourceMappingURL=prompt.js.map