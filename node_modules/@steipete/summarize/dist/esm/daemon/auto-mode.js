import { shouldPreferUrlMode } from '@steipete/summarize-core/content/url';
export function resolveAutoDaemonMode({ url, hasText }) {
    const preferUrl = shouldPreferUrlMode(url);
    const primary = preferUrl || !hasText ? 'url' : 'page';
    const fallback = primary === 'url' ? (hasText ? 'page' : null) : 'url';
    return { primary, fallback };
}
//# sourceMappingURL=auto-mode.js.map