import { loadSummarizeConfig } from '../config.js';
import { parseVideoMode } from '../flags.js';
import { parseOutputLanguage } from '../language.js';
import { parseBooleanEnv } from './env.js';
export function resolveConfigState({ envForRun, programOpts, languageExplicitlySet, videoModeExplicitlySet, cliFlagPresent, cliProviderArg, }) {
    const { config, path: configPath } = loadSummarizeConfig({ env: envForRun });
    const cliLanguageRaw = typeof programOpts.language === 'string'
        ? programOpts.language
        : typeof programOpts.lang === 'string'
            ? programOpts.lang
            : null;
    const defaultLanguageRaw = (config?.output?.language ?? config?.language ?? 'auto');
    const outputLanguage = parseOutputLanguage(languageExplicitlySet && typeof cliLanguageRaw === 'string' && cliLanguageRaw.trim().length > 0
        ? cliLanguageRaw
        : defaultLanguageRaw);
    const openaiWhisperUsdPerMinute = (() => {
        const value = config?.openai?.whisperUsdPerMinute;
        return typeof value === 'number' && Number.isFinite(value) && value > 0 ? value : 0.006;
    })();
    const videoMode = parseVideoMode(videoModeExplicitlySet
        ? programOpts.videoMode
        : (config?.media?.videoMode ?? programOpts.videoMode));
    const cliEnabledOverride = (() => {
        if (!cliFlagPresent || cliProviderArg)
            return null;
        if (Array.isArray(config?.cli?.enabled))
            return config.cli.enabled;
        return ['gemini', 'claude', 'codex'];
    })();
    const cliConfigForRun = cliEnabledOverride
        ? { ...(config?.cli ?? {}), enabled: cliEnabledOverride }
        : config?.cli;
    const configForCli = cliEnabledOverride !== null
        ? { ...(config ?? {}), ...(cliConfigForRun ? { cli: cliConfigForRun } : {}) }
        : config;
    const openaiUseChatCompletions = (() => {
        const envValue = parseBooleanEnv(typeof envForRun.OPENAI_USE_CHAT_COMPLETIONS === 'string'
            ? envForRun.OPENAI_USE_CHAT_COMPLETIONS
            : null);
        if (envValue !== null)
            return envValue;
        const configValue = config?.openai?.useChatCompletions;
        return typeof configValue === 'boolean' ? configValue : false;
    })();
    const configModelLabel = (() => {
        const model = config?.model;
        if (!model)
            return null;
        if ('id' in model)
            return model.id;
        if ('name' in model)
            return model.name;
        if ('mode' in model && model.mode === 'auto')
            return 'auto';
        return null;
    })();
    return {
        config,
        configPath,
        outputLanguage,
        openaiWhisperUsdPerMinute,
        videoMode,
        cliConfigForRun,
        configForCli,
        openaiUseChatCompletions,
        configModelLabel,
    };
}
//# sourceMappingURL=run-config.js.map