import type { Message } from '@mariozechner/pi-ai';
export type InputTarget = {
    kind: 'url';
    url: string;
} | {
    kind: 'file';
    filePath: string;
};
export type UrlKind = {
    kind: 'website';
} | {
    kind: 'asset';
};
export type AssetAttachment = {
    mediaType: string;
    filename: string | null;
    kind: 'image' | 'file';
    bytes: Uint8Array;
};
export declare function resolveInputTarget(raw: string): InputTarget;
export declare function classifyUrl({ url, fetchImpl, timeoutMs, }: {
    url: string;
    fetchImpl: typeof fetch;
    timeoutMs: number;
}): Promise<UrlKind>;
export declare function loadLocalAsset({ filePath, maxBytes, }: {
    filePath: string;
    maxBytes?: number;
}): Promise<{
    sourceLabel: string;
    attachment: AssetAttachment;
}>;
export declare function loadRemoteAsset({ url, fetchImpl, timeoutMs, maxBytes, }: {
    url: string;
    fetchImpl: typeof fetch;
    timeoutMs: number;
    maxBytes?: number;
}): Promise<{
    sourceLabel: string;
    attachment: AssetAttachment;
}>;
export declare function buildAssetPromptMessages({ promptText, attachment, }: {
    promptText: string;
    attachment: AssetAttachment;
}): Array<Message>;
