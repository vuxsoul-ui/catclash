import type { CliConfig, CliProvider } from '../config.js';
import type { ExecFileFn } from '../markitdown.js';
import type { LlmTokenUsage } from './generate-text.js';
type RunCliModelOptions = {
    provider: CliProvider;
    prompt: string;
    model: string | null;
    allowTools: boolean;
    timeoutMs: number;
    env: Record<string, string | undefined>;
    execFileImpl?: ExecFileFn;
    config: CliConfig | null;
    cwd?: string;
    extraArgs?: string[];
};
type CliRunResult = {
    text: string;
    usage: LlmTokenUsage | null;
    costUsd: number | null;
};
export declare function isCliDisabled(provider: CliProvider, config: CliConfig | null | undefined): boolean;
export declare function resolveCliBinary(provider: CliProvider, config: CliConfig | null | undefined, env: Record<string, string | undefined>): string;
export declare function runCliModel({ provider, prompt, model, allowTools, timeoutMs, env, execFileImpl, config, cwd, extraArgs, }: RunCliModelOptions): Promise<CliRunResult>;
export {};
