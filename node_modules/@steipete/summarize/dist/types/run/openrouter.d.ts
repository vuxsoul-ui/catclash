export declare function buildOpenRouterNoAllowedProvidersMessage({ attempts, fetchImpl, timeoutMs, }: {
    attempts: Array<{
        userModelId: string;
    }>;
    fetchImpl: typeof fetch;
    timeoutMs: number;
}): Promise<string>;
