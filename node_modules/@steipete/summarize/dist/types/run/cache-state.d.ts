import { type CacheState } from '../cache.js';
import type { SummarizeConfig } from '../config.js';
export declare function createCacheStateFromConfig({ envForRun, config, noCacheFlag, transcriptNamespace, }: {
    envForRun: Record<string, string | undefined>;
    config: SummarizeConfig | null;
    noCacheFlag?: boolean;
    transcriptNamespace?: string | null;
}): Promise<CacheState>;
export declare function refreshCacheStoreIfMissing({ cacheState, transcriptNamespace, }: {
    cacheState: CacheState;
    transcriptNamespace?: string | null;
}): Promise<boolean>;
